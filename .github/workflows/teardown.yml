name: ❌ tear down zoo

on:
  pull_request:
    types:
      - closed
    branches:
      - beacon-2690-single-repo-pr

jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: Testing
        run: echo Destroy
